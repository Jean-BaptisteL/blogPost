{% extends 'base.html.twig' %}

{% block title %}Hello ViewPostController!{% endblock %}

{% block body %}


<div class="row d-flex justify-content-center">
    <div class="col-6">
        <h2>{{ post.title }}</h2>
        <a href="{{ path('app_read_post_user', {'id': post.user.id}) }} ">Par {{ post.user.firstname }} {{ post.user.lastname }}</a>
        <p>{{ post.categoryId.name }}</p>
        <p>{{ post.content }}</p>
    </div>
</div>
{% endblock %}
