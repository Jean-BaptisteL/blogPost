<form method="post" action="{{ path('app_delete_category', {'id': category.id}) }}" onsubmit="return confirm('Êtes-vous sûr de voulor supprimer cette catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
    <button class="btn btn-danger text-while">Supprimer</button>
</form>